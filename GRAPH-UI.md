# 知识图谱前端改造计划（下钻式 + ECharts + 搜索）

## 目标
把 <your-domain> 的图谱可视化从"全量图"改为 **下钻式层级导航**，
使用 **Cytoscape.js**（早期 ECharts 因 graph 系列动态换图 dataIndex 崩溃 bug 弃用），
加模糊搜索高亮，打磨球体/文字显示。

## 你的核心需求（已确认）
1. 库：Cytoscape.js（取代初定的 ECharts）
2. 层级交互 = 下钻式：
   - 初始：顶层几个"大分类球"
   - 点某球 → 它成为中心，展开该球所在层级的新视图
   - 中心球周围只显示：它的**全部直系父球** + **全部直接子球**（带方向，1-hop）
   - 点子球 → 该子球成为新中心，继续下钻；可逐级返回
3. 球体与文字显示效果增强
4. 图角落搜索框：模糊搜索节点名，命中节点高亮

## 布局与数据语义约定
- edge 方向 = `head -> tail`（如"谷歌 --提出--> Transformer"），
  即 **head 视为父/上层，tail 视为子/下层**，箭头自父指向子。
- 中心球的"父球" = 有边 `父 -> 中心` 的节点；
  "子球" = 有边 `中心 -> 子` 的节点。
- 初始顶层球：选 **度数最高** 的一组节点（如 top 8）作为"大分类"。
  （也可按 kind 聚合出若干分类球 —— 见待确认项）

## 改动范围

### A. 后端 (app.py / db.py) —— 支持按节点下钻 + 搜索
新增/调整接口：
- `GET /graph/node/{id}`  返回以该节点为中心的局部子图：
  {center:{...}, parents:[{head,...,relation}], children:[{tail,...,relation}]}
  （只取直系父/子，不含祖父/孙）
- `GET /search_nodes?q=模糊词` 返回匹配节点列表（ILIKE %q%，可加排序）
- 保留现有 /graph（作为"全览/顶层视图"数据源，或改造为返回顶层 top-N 中心球）

### B. 前端 (web/ui.html 或新 web/graph.html)
用 ECharts graph 实现下钻导航：
- 状态机：当前中心节点 id + 历史栈（面包屑 / ← 返回按钮）
- 视图构建：
  - 顶层视图：画 top-N 中心球（大圆 + 分类名），提示"点击展开"
  - 下钻视图：中心球放中间(大)；父球依箭头排在中心上方/左侧，
    子球排在下方；球间画带方向边
- 显示增强：
  - 球体：按类型着色 + 渐变 + 关联度大小；中心球加光环/描边突出
  - 文字：球内/球旁标签，自动防重叠(密集时隐藏次要标签)、hover 放大
  - 布局：非对称辐射(父上子下)，ECharts circular/force 微调；下钻时动画过渡
- 搜索框：左下/右下角落，输入即模糊搜索，回车/选中高亮命中球
  （高亮：放大 + 高亮色 + 缩放聚焦到命中球）
- 返回/导航：面包屑显示路径，可回顶层

### C. 样式打磨
参考 ECharts 礼品级视觉：深色底、渐变球、描边、阴影、选中强调色、平滑动画。

## 文件组织
- 后端改动集中在 app.py / db.py（加 2 个接口）
- 前端改 web/graph.html（替换或新页面），主页入口指过去
- 所有说明 MD 存 ragkg 根（本文件 GRAPH-UI.md）

## 验收
- [x] 后端 /graph/node/{id}(直系父/子, 方向表修正, 去重) + /search_nodes(模糊)
- [x] 前端 graph.html(Cytoscape): 顶层 top-N -> 下钻(父上子下) -> 严格逐层 -> 返回/面包屑
- [x] 角落搜索框: 模糊匹配 -> 结果列表(类型图标) -> 点选高亮/下钻
- [x] <your-domain> 入口指向 graph.html
- [x] Playwright 无头浏览器实测: 下钻/续钻/返回/父球聚焦/搜索均通过, 无报错
  - 修复: 返回按钮原因"刚下钻 timeout disabled"失效(setBack 改用 currentCenter 判断)
- [x] 详情面板「来源文档」可点击 → 新标签 /doc.html?id= 渲染打开 (md/pdf/txt; 无物理文件时重组全文)
- [ ] 浏览器人工最终验收(视觉效果/交互手感)

## 待确认项 → 已锁定决策
- 顶层"大分类球"：取**度数 top-N**（如 top 8）的中心实体作为分类球。
- 方向修正：维护**关系词方向表** —— head 为父（提出/创建/开发/拥有/推动/主导/成立...）；
  反转语义的词（使用/属于/位于/采用/依赖...）head 实为子 → 绘制与"父子"计算时纠正。
- 搜索命中多个：弹**结果列表(带类型图标)**，点选一条才下钻/高亮；未点选前仅轻量高亮。
- 下钻层级：**严格逐层** —— 每层只显示当前中心的直系父+子；必须点子球才进下一层；可返回。

## 实现细节
- 后端 /graph/node/{id}：按方向表把边归类为 parent(父边) / child(子边)，
  返回 {center, parents[], children[]}（只直系邻域）。
- 前端下钻状态机：center + hist 栈(返回) + 面包屑；每层 force/circular 辐射布局(父上子下)。
- 搜索：/search_nodes 模糊匹配；前端结果下拉列表(类型图标) → 点选 → 高亮聚焦 or 下钻该球。